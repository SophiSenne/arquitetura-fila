version: "3.9"

services:
  db:
    build: ./database
    container_name: postgres_db
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: